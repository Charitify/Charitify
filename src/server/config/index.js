const userSexEnum = ["male", "female", "unset"];

const userRoles = {
  user: "user",
  admin: "admin",
  moderator: "moderator",
};

const userRolesEnum = Object.values(userRoles);

export { userSexEnum, userRoles, userRolesEnum };
